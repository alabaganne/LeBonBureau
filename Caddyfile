# HTTPS automatique (Let's Encrypt) pour la boutique et l'API Medusa.
{$DOMAIN} {
	encode gzip
	reverse_proxy storefront:3000
}

www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}

{$API_DOMAIN} {
	encode gzip
	reverse_proxy medusa:9000
}
